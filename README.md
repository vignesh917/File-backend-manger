# File-backend-manger
This project is a RESTful backend API for managing files, developed using Node.js and Express. It allows users to upload files, store their metadata in a MySQL database, retrieve a list of uploaded files, download them, and delete them from both the server and database.
